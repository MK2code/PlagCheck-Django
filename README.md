# PlagCheck-Django

## Local Setup
1. Copy `.env.example` to `.env`:
   ```bash
   cp .env.example .env
   ```
2. Fill in the missing environment variables in `.env` like secret key, email credentials, and OCR tool paths.